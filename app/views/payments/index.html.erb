<div class="payment-show-photo">
  <div class="message-color">

    <p id="notice"><%= notice %></p>

    <h1>履歴確認</h1>

    <table>
      <thead>
        <tr>
          <th>日にち</th>
          <th>分類</th>
          <th>金額</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @payments.each do |payment| %>
          <tr>
            <td><%= payment.input_date.strftime("%Y年%m月%d日") %></td>
            <td><%= payment.category_i18n %></td>
            <td><%= payment.money %></td>
            <td><%= link_to '編集する', edit_payment_path(payment) %></td>
            <td><%= link_to '削除する', payment, method: :delete, data: { confirm: '本当に削除しても良いですか？' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <a>開始月と終了月を選択して、範囲内の分類別累計金額が表示できます。<a>
    <%= form_with url: payment_path, html: {method: 'GET'} do |form| %>
      <%= form.label :開始月を選択 %>
      <%= form.date_select :iuput_date, {discard_day: true} %>
    <% end %>
    <%= form_with url: payment_path, html: {method: 'GET'} do |form| %>
      <%= form.label :終了月を選択 %>
      <%= form.date_select :input_date, {discard_day: true} %>
      <%= form.submit "累計表示" %>
    <a>ここに分類別累計金額を表示</a>
    #i = -1
    #i += 1 <14まで
    #13カテゴリ分表示させる
    <% end %>
  </div>
  <img src="/assets/the-honest-company-_UzLeRFhUPI-unsplash.jpg" width="100%" height="100%">
</div>